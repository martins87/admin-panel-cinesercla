export type Pergunta = {
  id: string;
  pergunta: string;
  resposta: string;
  cadastro: string;
  cliques: string;
  ordem: string;
  categoria: string;
  principalDuvida: boolean;
  ordemPrincipalDuvida: string;
  ativa: boolean;
};
